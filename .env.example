# URL base de la API backend (sin trailing slash)
# Con Docker Compose apuntá al servicio o IP de tu backend
# Ej: http://host.docker.internal:8000/api/v1 (acceso al host desde Docker en Mac/Windows)
VITE_API_BASE_URL=http://localhost:8000/api/v1
